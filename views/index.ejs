<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Poshidaraj Blog</title>
    <link rel="stylesheet" href="/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-B1EY33XFNS"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-B1EY33XFNS');
</script>
</head>
<body class="bg-zinc-100">
    <%- include('partials/header', { user: user }) %>

    <!-- Quote Section -->
    <section class="bg-zinc-200 py-6 text-center">
        <blockquote class="text-lg italic font-semibold text-gray-800">
            "Enhancing Marital Bliss through Islamic Principles"
        </blockquote>
    </section>

    <main class="container mx-auto p-4">
        <h2 class="text-3xl font-bold mb-4 text-center text-gray-900">पोशीदाराज अब हिंदी में</h2>
        <div id="postList" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% posts.forEach(function(post, index) { %>
                <div class="bg-white p-6 rounded-lg shadow-lg transition-transform duration-300 ease-in-out transform hover:shadow-xl hover:scale-105">
                    <h3 class="text-xl font-semibold mb-2 text-gray-900"><%= post.title %></h3>
                    <p class="text-gray-700 mb-4"><%= post.content.substring(0, 100) %>...</p>
                    <a href="/post/<%= index %>" class="text-blue-600 hover:underline">Read More</a>
                </div>
            <% }); %>
        </div>
    </main>

    <%- include('partials/footer') %>
</body>
</html>
